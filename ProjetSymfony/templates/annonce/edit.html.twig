{% extends 'base.html.twig' %}

{% block content %}
    <p class="marge"></p>
    <div class="new-annonce edit-annonce">
        <h1>Modifier votre annonce</h1>

        {{ include('annonce/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('app_annonce') }}" class="back-btn">Retour à mes annonces</a>

        {{ include('annonce/_delete_form.html.twig') }}
    </div>
{% endblock %}
